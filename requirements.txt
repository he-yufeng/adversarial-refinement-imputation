# Dependencies for the MiLeTS 2026 paper (the FMGAN study).
#
# Reproducing the paper tables (`python3 FMGAN/analysis.py`) needs only the
# Python standard library + the bundled results JSON — no install required.
#
# Re-running experiments (training the R3GAN-1D refiner, the PyPOTS baselines,
# the MOMENT coarse imputer) needs the full stack below and a GPU.

-r FMGAN/requirements.txt
